# TASK-WP04-02-VERIFIER-HARNESS-R5-CODEX-TAKEOVER-R2 — Execution Report

STATUS: IMPLEMENTATION_COMPLETE

This is executor evidence, not independent acceptance. It closes only the two R2 repair targets and preserves the prior R1 implementation and failed R1 evidence for a separate Codex verifier.

## Fixed working state

- Worktree: `/Users/qianduoduo/Desktop/AI_app/codex-wp04-02-verifier-harness-r5-repair`
- Branch: `codex/wp04-02-verifier-harness-r5-repair`
- HEAD remained `df836cb39a1234aae967553b3783a67df9ad6672`; parent remained `675217c3a15c0f416aa4462ca6edc491bf99f9f6`.
- Main remained `675217c3a15c0f416aa4462ca6edc491bf99f9f6`.
- Candidate remained clean at `codex/wp04-02-evidence-domain-service@e942cbcc9e0b5d95a6c7ee46d4d74ee90ff03ad4`; parent remained `af4f2cbbb0a50bd6c215bcaa78dd9fcd98414cc8`.
- The R1 report, R1 evidence, historical zcode report/evidence, and pre-existing plugin modification were not rewritten. Before/after hashes are recorded in `baseline-state.json` and `final-state.json`.

## R2 change inventory

Only the two permitted source/test files changed during R2:

1. `tg_verifier_tools/verification/wp04_02_r4_runner.py`
2. `tg_verifier_tests/verification/test_wp04_02_r4_runner.py`

The only R2 additions are this report and `docs/acceptance/TASK-WP04-02-VERIFIER-HARNESS-R5-CODEX-TAKEOVER-R2-evidence/`. The pytest plugin was not changed during R2; its SHA-256 remained `3ab4b45feb356fcecdaa5363f09d3256dc0c6a2b8f2bb067d32da8438f3bdbc4`.

## AC-R2-01 — inherited credential non-discovery

Implementation:

- `wp04_02_r4_runner.py:766-788` initializes the pre-child redaction set as empty and no longer copies or enumerates `os.environ`.
- `wp04_02_r4_runner.py:675-711` constructs the child environment from a fixed process-basic allowlist plus runner-owned values. The database guard URL is the hard-coded synthetic loopback port-1 value at line 81; the ledger path is derived from the fresh evidence leaf. Inherited database URLs, passwords, tokens, DSNs, auth values, cookies and sessions are not consulted.
- `wp04_02_r4_runner.py:870-876` collects redaction literals only after the explicit minimal child environment exists.
- `wp04_02_r4_runner.py:1072-1091` accepts an explicit guard directory, resolves it to an existing directory and does not source it from inherited `PYTHONPATH`.

Regression coverage:

- `test_wp04_02_r4_runner.py:428-463` proves malicious inherited database/ledger values are replaced with verifier-owned values and an unrelated sensitive variable is absent from the child environment.
- `test_wp04_02_r4_runner.py:466-530` runs `run_collect_only` through an early main-identity failure using an environment object that raises on enumeration, copy, sizing, key/item traversal or access to the unrelated sensitive key. It also scans retained evidence for both the synthetic key and value.
- The independent executable counterexample record is `inherited-environment-non-access.json`: zero inherited keys accessed, zero collector calls before the child environment, and zero retained key/value occurrences.

Runtime evidence:

- Exactly one positive R2 collect-only was launched under `env -i` with an explicit fail-closed socket guard.
- CLI exit and pytest return were 0; the current invocation nonce matched.
- Collection retained 41 unique node IDs with exact distribution `20 / 16 / 3 / 2` and `test_body_calls == 0`.
- The runner-owned fixture ledger did not exist, the socket-attempt log did not exist, and the evidence scan found zero CREATE activity.
- No real credentials were supplied and no real PostgreSQL operation was authorized or performed.
- Live/staged/recorded/retained plugin hashes and live/retained runner hashes were equal in the positive bundle.
- `guarded-no-db-proof.json`, `harness-run/result.json`, `harness-run/collect-only.meta.json`, `harness-run/collected-nodeids.json`, and `harness-run/source-hashes.json` contain the exact evidence.

## AC-R2-02 — complete recursive outer manifest

Implementation:

- `wp04_02_r4_runner.py:509-527` excludes only the exact root `manifest.json`; nested files with that basename remain normal artifacts.
- `wp04_02_r4_runner.py:530-571` mechanically recomputes actual recursive paths, set equality, byte equality and SHA-256 equality.
- `test_wp04_02_r4_runner.py:594-617` locks the nested-manifest counterexample.

R2 outer evidence result:

- Actual retained files, excluding only the exact outer root manifest: 51.
- Outer manifest entries: 51.
- Inventory equality: true.
- Byte/SHA-256 equality: true.
- Missing entries: none.
- Extra entries: none.
- Size/hash mismatches: none.
- Final outer manifest SHA-256: `6f43578d74ab4014af9aa7e7115dee68cd8e0d28a17d9ff92fc6b77ab991e73f`.
- The manifest was generated, audited, then regenerated byte-identically as the final evidence write. No R2 evidence file was modified afterward.

Nested manifests explicitly retained by the outer manifest:

1. `harness-run/manifest.json`
2. `negative/controlled-interruption/manifest.json`
3. `negative/embedded-url-redaction/manifest.json`
4. `negative/staged-mismatch/manifest.json`

The mechanical result is recorded in `outer-manifest-audit.json` and is independently reproducible with `_audit_recursive_manifest` against the outer `manifest.json`.

## Preserved negative behavior

- Controlled interruption: CLI exit 1, structured `interrupted`, subprocess return 130, closed 9-artifact inner bundle.
- Staged mismatch: CLI exit 1, `staged_plugin_mismatch`, pytest not started, closed 6-artifact inner bundle.
- Embedded credential URL: CLI exit 1, structured startup failure, closed 9-artifact inner bundle. Repository-wide R2 evidence scanning found zero retained occurrences of the synthetic URL username, password or token.
- Every negative inner manifest has exact inventory, byte and SHA-256 equality.

## Required commands

The final source was exercised with the contract commands:

- Focused pytest: 82 collected, 82 successful, no skip or weakening; output in `focused-tests.txt`.
- `ruff check --no-cache`: exit 0.
- `ruff format --check --no-cache`: both files already formatted.
- `python -m compileall`: both allowed files compiled; `PYTHONPYCACHEPREFIX` redirected bytecode to `/private/tmp` because the worktree filesystem is read-restricted to shell commands.
- `git diff --check`: exit 0 with no output.

The exact quality outputs are in `quality-checks.txt`. The TDD RED counterexample is in `tdd-red.txt`.

## Delivery accounting

- Repair count: 1 controlled R2 repair iteration (tests added RED, minimal implementation applied, complete suite green).
- Positive collect-only count: exactly 1.
- Negative evidence runs: 3 required scenarios.
- Measured mutation/verification/evidence interval: `2026-09-21T08:11:18Z` through `2026-09-21T08:19:40Z` (`00:08:22`). Required-context reading time is not included in that measured interval.
- Visible token usage: `UNKNOWN`.
- Visible cost: `UNKNOWN`.
- Acceptance levels requested for the separate verifier: `L1_STATIC_REVIEWED`, `L2_BUILD_VERIFIED`, `L3_CONTRACT_VERIFIED`, collect-only `L4_RUNTIME_VERIFIED`.
- `L4_DB_VERIFIED` was neither required nor authorized.

Awaiting a separate Codex independent acceptance. No real PostgreSQL task, merge, push, 05C-02/R1D or WP-04-03 work is authorized by this delivery.
